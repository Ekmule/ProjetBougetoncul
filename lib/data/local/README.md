Tables Drift implémentées en D3 :

- `tasks` — via `TaskEntries` / `TaskRow`
- `sync_operations` — file de sync (D15)
- `user_settings` — préférences (D12+)

Génération de code :

```powershell
dart run build_runner build
```
